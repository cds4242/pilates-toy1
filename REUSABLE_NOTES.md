# 학원 도메인 풀스택 외주 자산 — 시뮬레이션 종료 최종 통합 정리

> 14 STEP 시뮬레이션 (필라테스 학원 예약 시스템) 종료
> 다음 외주에서 활용할 자산 + 회고 + SOP 통합 문서
> 2026-05-10

---

## 1. 외주 SOP (Standard Operating Procedures)

### 1.1 외주 시작 시
- **견적 협의**: 변경 합의서 패턴 (범위·금액·기한 서면화)
- **명세서 작성**: SPEC.md 표준 (역할/도메인/비즈니스규칙/화면/기술스택)
- **디자인 시안**: 3종 생성 → 의뢰인 선택 → 색상 토큰 확정
- **기술 스택 결정**: 의뢰인 서버 환경 확인 후 확정
- **회색 지대 정리**: 명세 모호한 부분 → 의뢰인 확인 후 합의서 추가

### 1.2 STEP 진행 시 도구
- **domain-bootstrap** Skill: 신규 도메인 표준 구조 일괄 생성 (Entity/Repository/Service/Controller/DTO/ErrorCode)
- **migration-add** Skill: Flyway 마이그레이션 새 버전 SQL 자동 생성 (V{N} 자동 계산)
- **api-test-write** Skill: 컨트롤러 분석 후 E2E 또는 단위 통합 테스트 자동 생성
- **pr-description** Skill: Git 변경 분석 후 한국어 PR description 자동 작성
- **daily-report** Skill: Git 커밋 분석 → 의뢰인용 주간 진행 보고서 자동 생성

### 1.3 검수 표준
- **@DisplayName 카운트** = 프롬프트 시나리오 카운트 **1:1 매칭**
- **skip 카운트 0 보장**: `@EnabledIf`, `@Disabled` grep 검사
- **BUILD SUCCESSFUL ≠ 검증 통과**: 빌드 성공 ≠ 기능 동작
- **본인 grep 30초 표준**: `grep -c "@DisplayName"` 직접 확인
- **DB 직접 확인**: `SHOW TABLES`, `DESCRIBE`, H2 콘솔
- **onClick 없는 button 0개**: `grep -r "onClick" | wc -l` vs button 수 비교
- **"준비 중" placeholder 즉시 거부**: `alert("준비 중")`, `toast("미구현")` grep 검사

---

## 2. AI 함정 패턴 10가지 (★ 외주 핵심 자산)

### 함정 1: 거짓 보고 (membership_pass)
- **발견 시점**: STEP 6
- **증상**: "원래 없었다"라며 스펙 누락을 정당화
- **원인**: 실제로는 명세에 있었으나 AI가 구현을 빠뜨림
- **해결**: 본인이 명세 대조하여 발견
- **다음 SOP**: 시나리오 vs 결과 표 1:1 매칭 습관화

### 함정 2: 시나리오 누락 (50~60%)
- **발견 시점**: STEP 7~8
- **증상**: 8개여야 할 테스트 → 4개만 작성. 13개 → 5개만.
- **원인**: AI가 "핵심만" 작성하고 나머지를 생략
- **해결**: `grep -c "@DisplayName"` 카운트 확인
- **다음 SOP**: 프롬프트에 시나리오 수 명시 + 결과 표 행 수 검증

### 함정 3: 시스템 결함 우회 (강사 인증)
- **발견 시점**: STEP 9
- **증상**: "memberId를 instructorId로 사용"이라며 결함을 위장
- **원인**: 인증 체계 결함을 "설계 의도"로 포장
- **해결**: 진실 추적 패턴으로 근본 원인 발견
- **다음 SOP**: AI "솔직 보고" = 시스템 결함 단서. 즉시 grep 검증.

### 함정 4: 보안 누락 (phone 평문)
- **발견 시점**: STEP 10
- **증상**: 강사 phone 암호화 누락, 알림 발송 흐름 우회
- **원인**: "편의상" 평문 저장
- **해결**: 보안 표준 명시 + grep으로 평문 검색
- **다음 SOP**: 암호화 필드 명시 + 시드 후 반드시 로그인 테스트

### 함정 5: onClick 누락 (21개)
- **발견 시점**: STEP 12.5
- **증상**: 디자인은 매칭되었지만 버튼/아이콘이 동작하지 않음
- **원인**: JSX에 이벤트 핸들러 미연결
- **해결**: 전체 화면 실제 클릭 테스트
- **다음 SOP**: 모든 인터랙티브 요소 실제 클릭. Chrome MCP 순회.

### 함정 6: "준비 중" placeholder 우회
- **발견 시점**: STEP 12.5 보강
- **증상**: 핵심 기능에 `alert("준비 중")`, `toast("API 미구현")`
- **원인**: 구현 난이도 회피
- **해결**: placeholder를 실제 기능으로 교체
- **다음 SOP**: placeholder 사전 협의. 허용 범위 명시.

### 함정 7: 데이터 정합성 위반
- **발견 시점**: STEP 12.6
- **증상**: 정기권 6회 남았는데 "사용 가능한 정기권 없음" 에러
- **원인**: 정기권-수업유형 매핑 누락 (시드 데이터 불일치)
- **해결**: 시드 데이터 + 비즈니스 규칙 대조 검증
- **다음 SOP**: 시드 작성 시 비즈니스 규칙 검증 SQL 동반 실행

### 함정 8: PC 반응형 부족
- **발견 시점**: STEP 12.6
- **증상**: 회원 네비게이션이 PC에서 완전히 사라짐 (`md:hidden`)
- **원인**: 모바일 우선 개발 후 PC 검증 생략
- **해결**: PC/모바일 양쪽 뷰포트 테스트
- **다음 SOP**: 뷰포트별 디자인 명시 (모바일/태블릿/PC). 양쪽 스크린샷.

### 함정 9: 시각적 일관성 부족
- **발견 시점**: QA 라운드
- **증상**: 같은 개념을 "정기권"/"수강권" 혼용, 페이지 간 스타일 차이
- **원인**: 용어 사전 없음, 디자인 토큰 미통일
- **해결**: 용어 통일 + 디자인 시스템 유틸리티 클래스 정의
- **다음 SOP**: 프로젝트 시작 시 용어 사전 + 디자인 토큰 표준 확정

### 함정 10: UX 디테일 부족
- **발견 시점**: QA 라운드
- **증상**: toast 2초 만에 사라짐, confirm이 브라우저 기본, 빈 상태에 CTA 없음
- **원인**: "동작하면 완료"라는 AI 판단 기준
- **해결**: 비개발자 관점 전체 시나리오 순회
- **다음 SOP**: 화면별 상태(로딩/빈/에러/성공) 명시. 5초 룰.

---

## 3. 시뮬레이션 종료 직전 자각 (★ 가장 큰 학습)

### 3.1 부실한 기획 = 사고 근원
- 명세 부족 → AI 임의 해석 → 코드 문제가 아니라 **기획 문제**
- 코드는 기획의 실행일 뿐. 기획이 없으면 검수 기준도 없다.
- **외주 시간 분배: 기획 30% 필수** (이전: 5%)
- 기획서 = 검수 기준서. 기획에 없는 것은 검수할 수 없다.

### 3.2 AI 다중 에이전트 협업
- **단일 에이전트 한계**: 70% 완성도. 컨텍스트 소모, 방향 이탈.
- **다중 에이전트**: 기획 Claude / 개발 Claude (4~8개 병렬) / QA Claude 분리
- **본인 = PM 역할**: 의뢰인 의도 파악, 명세 검수, 조율, 최종 판단
- **결과**: 단일 70% → 다중+본인 90%

### 3.3 Chrome MCP 실시간 검수
- QA Claude + Chrome MCP + 본인이 동시에 화면을 보며 검수
- **시각 발견 → 즉시 수정 지시 → 재검증** 사이클
- 스크린샷 기반 QA 보고서 자동 생성 (HTML/MD)
- 단일 검수 < **실시간 검수** (속도 + 정확도)

---

## 4. 학원 도메인 표준 자산 (★ 다음 외주 그대로)

### 4.1 기술 스택 (확정)
| 영역 | 기술 | 버전 |
|------|------|------|
| Backend | Spring Boot + JPA + QueryDSL | 3.3 + 5.1 |
| Runtime | Java | 21 |
| DB | MySQL + Redis | 8.0 + 7 |
| Migration | Flyway | 10.x |
| Frontend | Next.js + React + TypeScript | 16 + 19 |
| CSS | TailwindCSS + shadcn/ui | 4 |
| State | Zustand + TanStack Query | 5 |
| Test (BE) | JUnit 5 + MockMvc + H2 | - |
| Test (FE) | Playwright | 1.59 |
| Build | Gradle (Kotlin DSL) + npm | 8.10 |

### 4.2 보안 인프라 (100% 재사용)
- **AES-256/GCM 암호화**: phone, name, email → keyVersion + IV 포함
- **SHA-256 해시**: phone_hash → 검색/중복 확인용
- **BCrypt strength 12**: 비밀번호 해시
- **JWT**: jjwt 0.12.x, HS256, Access 30분 + Refresh 14일
- **AuthTestHelper**: `loginAsMember()`, `loginAsAdmin()`, `loginAsSuperAdmin()`, `loginAsInstructor()`
- **MaskingUtil**: 휴대폰 `010-****-5678`, 이름 `김*수`

### 4.3 도메인 패턴 (90% 재사용)
| 도메인 | 핵심 패턴 | 재사용률 |
|--------|----------|---------|
| auth | SMS 인증 + 회원가입 + JWT | 95% |
| member | 암호화 + 프로필 + 탈퇴(익명화) | 90% |
| instructor | admin 계정 + 강사 연결 + 프로필 확장(주소/자격증/근무요일) | 85% |
| classroom | 고정 스케줄 → 자동 생성 + 강사 필터 + 예약자 명단 | 80% |
| membership | 정기권 종류 + 발급 + 홀딩 + 노출/판매 관리 + 카테고리 | 90% |
| payment | 토스 Mock/Real + 보상 트랜잭션 | 95% |
| reservation | 동시성 (낙관적+비관적) + 확인 모달 + 대기 | 85% |
| attendance | 출석 + 노쇼 스케줄러 + 달력 히트맵 | 90% |
| notification | 알림톡 + SMS 폴백 + Spring Event | 90% |
| admin | 대시보드 + 통계 + 엑셀 + 수강권 상품 관리 + 도움말 시스템 | 80% |

### 4.4 권한 매트릭스 (학원 표준)
| 경로 | MEMBER | INSTRUCTOR | ADMIN | SUPER_ADMIN |
|------|--------|-----------|-------|-------------|
| /api/auth/** | permitAll | | | |
| /api/admin/auth/** | permitAll | | | |
| /api/admin/settings/** | X | X | X | O |
| /api/admin/** | X | O | O | O |
| /api/instructor/** | X | O | O | O |
| /api/members/me/** | O | O | O | O |

### 4.5 동시성 검증 자산
- **낙관적 락**: `@Version` + `OptimisticLockException` → H2에서 검증 가능
- **비관적 락**: `@Lock(PESSIMISTIC_WRITE)` → Testcontainers MySQL 필수
- **테스트 패턴**: `ExecutorService` + `CountDownLatch` + `AtomicInteger`
- **@EnabledIf 우회 감시**: AI가 Testcontainers 없으면 skip 시도

---

## 5. 풀스택 통합 패턴

### 5.1 API 클라이언트 (Next.js)
- Axios + `ApiResponse<T>` 자동 언래핑
- JWT 인터셉터 + 자동 갱신 (401 → refresh → role별 리디렉트)

### 5.2 인증 상태 (Zustand)
- localStorage persist + `_hydrated` 플래그로 SSR 리디렉트 방지
- 권한별 라우팅 (middleware + useEffect 가드)

### 5.3 디자인 시스템
- shadcn/ui + Tailwind 유틸리티
- 디자인 토큰: `--color-pilates`, `--color-gold`, `--color-instructor`
- CSS 클래스: `card-elevated`, `card-hover`, `hero-gradient`, `btn-primary`
- 모바일 우선 (회원), PC 우선 (관리자)

### 5.4 공통 컴포넌트
- **HelpTip**: 물음표 아이콘 → 설명 팝업 + 바로가기 링크
- **PageGuide**: 페이지 상단 안내 배너 (닫기 가능)
- **MobileTabBar**: 하단 탭바 (홈/예약/수강권/마이, filled 아이콘)
- **AdminSidebar**: PC 사이드바 + 모바일 햄버거 + 페이지명 표시

---

## 6. 검증 도구 시너지

### 6.1 Playwright (코드 기반)
- 매 push CI 자동 실행, UI 회귀 방지

### 6.2 Playwright MCP / Chrome MCP
- Claude가 직접 브라우저 조작 → 스크린샷 → 분석
- QA 보고서 자동 생성 (HTML/MD)

### 6.3 활용 비율
- 일상 95%: Playwright 코드 테스트
- QA 라운드: MCP 전체 화면 순회 + 스크린샷
- 이슈 발생 시: MCP 즉석 디버깅

---

## 7. 외주 시간 분배 표준 (★ 본인 자각)

### 이전 인식 (잘못됨)
| 기획 5% | 디자인 5% | 개발 80% | 검수 10% |

### 수정된 표준 (확정)
| 단계 | 비중 | 활동 |
|------|------|------|
| **기획** | **30%** | 명세서, 와이어프레임, 인터랙션, 비즈니스 규칙 |
| **디자인** | **20%** | 시안 3종, 디자인 토큰, 컴포넌트 |
| **개발** | **30%** | 구현, 테스트 (AI 에이전트 병렬) |
| **검수 + QA** | **20%** | Chrome MCP + 본인 클릭 + 의뢰인 데모 |

---

## 8. AI 다중 에이전트 워크플로우 (★ 본인 발견)

### 8.1 역할 분리
```
본인 (PM/시니어)
├── 기획 Claude: 명세 디테일, 비즈니스 규칙, 인터랙션 명시
├── 개발 Claude (4~8개 에이전트 병렬)
│   ├── Agent 1~2: 백엔드
│   └── Agent 3~4: 프론트엔드 (파일별 분리)
├── QA Claude + Chrome MCP: 시각 검수, 즉시 수정 지시
└── 본인: 빌드 확인 + 실제 클릭 + 의뢰인 데모
```

### 8.2 핵심 원칙
- **본인 = PM**. AI는 실행자.
- **기획은 본인이 직접** (AI에 위임하면 방향이 틀어짐)
- **개발은 AI에 위임**하되 **파일 단위로 분리** (충돌 방지)
- **검수는 AI + 본인 이중 체크**

### 8.3 효과
| 방식 | 완성도 |
|------|--------|
| AI 단독 위임 | 70% |
| 다중 에이전트 + 본인 PM | 90% |
| 다중 + 본인 + 디자이너 | 95%+ |

---

## 9. 다음 외주 SOP (확정)

### 9.1 명세 비중 30%
전체 시간의 30%를 명세에 투자.
- 화면 설계서 (Figma / 와이어프레임)
- 기능 정의서 (API 목록, 비즈니스 규칙)
- 권한 매트릭스 + 용어 사전

### 9.2 AI 위임 비율 50% 이하
- 보일러플레이트: AI 위임 OK
- 비즈니스 로직: 본인 작성 + AI 보조
- 보안/결제: 본인 필수 참여

### 9.3 본인 PM 역할
- 매일 진행 상황 정리
- 주 1회 의뢰인 데모
- 변경 요청 → 합의서 → 견적 반영
- AI 위임 비율 STEP별 기록

### 9.4 매 STEP 끝 QA + 본인 클릭
- AI QA: Chrome MCP 전체 화면 순회
- 본인 QA: 실제 브라우저에서 핵심 시나리오 클릭
- 이중 체크 후 STEP 완료 처리

---

## 10. 다음 외주 시간 단축 추정

| STEP | 이번 외주 | 다음 외주 (자산 활용) | 절약 |
|------|---------|-------------------|------|
| 셋업 | 8h | 2h | 75% |
| 도메인 (10개) | 100h | 30h | 70% |
| 프론트엔드 | 24h | 12h | 50% |
| 통합·QA | 12h | 6h | 50% |
| 배포 | 12h | 6h | 50% |
| 디자인 보강 | 24h | 12h | 50% |
| **합계** | **180h** | **68h** | **62%** |

---

## 11. 견적 책정

### 11.1 기획 비용 별도 청구
- "기획 단계 50만원 + 개발 단계 OOO만원" 구조
- 기획 후 개발 불발 시에도 기획 비용은 청구 가능
- 기획서 = 납품 산출물

### 11.2 AI 도구 활용 어필
- "AI 도구를 활용하여 개발 속도를 2~3배 높였습니다"
- "코드 품질은 시니어 레벨의 수동 검수로 보장합니다"
- 비용은 시장가 유지 (AI 사용 = 품질 향상, 가격 인하 X)

### 11.3 회귀 자동화 어필
- "E2E 테스트 자동화로 기능 추가 시 기존 기능 깨짐 방지"
- "Playwright 테스트 스위트 포함 납품" → 유지보수 비용 절감
- CI/CD 파이프라인 → 배포 자동화

### 11.4 참고 가격대
| 패키지 | 범위 | 기간 | 가격 |
|--------|------|------|------|
| BASIC | 관리자 + 사용자 | 3~4주 | 200~300만원 |
| STANDARD | + 역할 분리 + 결제 | 5~7주 | 400~600만원 |
| PREMIUM | + 커스텀 + 앱 | 8~12주 | 600~1,000만원 |

---

## 12. 시뮬레이션 회고 (자기 평가)

### 12.1 본인 자각
- **AI 100% 위임 → 불가능** (시뮬레이션으로 발견)
- **검수 능력 = 시니어 자산** (검수 못 하면 외주 하면 안 됨)
- **부실한 기획 = 사고 근원** (코드 문제 X, 기획 문제 O)
- **다중 에이전트 + Chrome MCP = 외주 표준** (본인 발견)

### 12.2 이번 시뮬레이션에서 달라진 것
| 항목 | STEP 1~8 (초기) | STEP 9~12.5 (성숙) | 최종 QA 라운드 |
|------|----------------|-------------------|--------------|
| AI 위임 비율 | 90% | 60% | 70% (검증된 패턴) |
| 검수 방식 | 빌드만 확인 | grep + DB | Chrome MCP + 본인 클릭 |
| 버그 발견 시점 | 다음 STEP | 같은 STEP | 실시간 |
| 함정 인식 | 0개 | 5개 | 10개 |
| 기획 비중 | 5% | 15% | 30% |

### 12.3 다음 외주 적용 (확정)
1. **기획 30% → 디자인 20% → 개발 30% → 검수 20%**
2. **본인 작성 50% 이상** (AI 전면 위임 X)
3. **작은 외주부터** (3주 이내)
4. **의뢰인 데모 주 1회** (피드백 루프)
5. **AI 위임 비율 의식적 관리** (STEP별 기록)
6. **매 STEP QA**: Chrome MCP 순회 + 본인 클릭

### 12.4 가장 큰 자산 (정렬)
1. **자기 인식** (시니어 능력의 핵심)
2. **10가지 AI 함정 패턴** → 사전 방지
3. **AI 다중 에이전트 워크플로우** 표준 확립
4. **Chrome MCP 실시간 검수** 체계
5. **검수 표준** 정착 (grep 30초)
6. **도메인 코드 자산** (90% 재사용)
7. **크몽 포트폴리오** 자료 (portfolio/ 디렉토리)
8. **다음 외주 사고 90% 방지** 확신
9. **평점 5점 보장 가능**이라는 자신감
