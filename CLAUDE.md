# CLAUDE.md — 필라테스 프로젝트 작업 지침

이 파일은 Claude Code가 이 저장소에서 작업할 때 따라야 하는 규칙을 담는다.
프로젝트 문서는 다음을 참조:
- `SPEC.md` — 기능 명세
- `STANDARDS.md` — 코드 표준
- `PROJECT_STRUCTURE.md` — 디렉토리 구조
- `WORKLOG.md` — 세션 간 작업 로그
- `DECISIONS.md` — 주요 의사결정 기록

---

## 작업 가드레일 (필수)

이 프로젝트는 본인이 시뮬레이션을 통해 발견한 함정 10개를 방지하는 게 핵심이다.
모든 작업 시 다음 규칙을 따른다.

### 절대 금지
- "준비 중" placeholder (alert/toast/console.log) 절대 금지
- @EnabledIf, @Disabled로 테스트 skip 절대 금지
- 시나리오 일부만 구현 후 "핵심만 작성"이라 보고 금지
- onClick 없는 button 금지
- BUILD SUCCESSFUL을 검증 통과로 보고 금지
- TODO 주석 잔존 (전부 처리 또는 명시적 v2 마킹)

### 작업 완료 정의
- 시나리오 카운트 = @DisplayName 카운트 (1:1 매칭)
- 모든 button은 onClick 연결 또는 명시적 미사용 표시
- 구현 완료 보고 시: 다음 grep 결과 첨부 필수
  * `grep -c "@DisplayName" [경로]` — 시나리오 카운트
  * `grep -rn "준비 중\|미구현\|TODO" [경로]` — 잔존 placeholder
  * `grep -c "onClick" [페이지 경로]` — 버튼 연결 확인

### 의심스러운 보고 패턴 (자가 감지)
다음 표현이 나오면 즉시 멈추고 본인에게 확인 요청:
- "원래 명세에 없었습니다" → 명세 재확인 필요
- "memberId를 instructorId로 사용" 류의 우회 → 결함 단서
- "편의상" "임시로" "일단" → 즉시 정상화 요구
- 시나리오 N개 → 결과 N-X개 → 누락 사유 명시

### 보고 형식 (필수)
모든 작업 완료 보고 시:
1. 한 것 (3~5줄)
2. 검증 결과 (grep + build + test)
3. 누락·미완료 솔직 보고 (X면 X라고 명시)
4. 다음 결정 필요한 것 (있으면)

---

## 세션 동기화 규칙 (필수)

### 작업 시작 시
1. WORKLOG.md 마지막 10줄 읽기
2. DECISIONS.md 전체 스캔 (해당 작업과 관련된 결정 있는지)
3. 직전 세션의 "다음 결정 필요" 항목 확인

### 작업 종료 시
1. WORKLOG.md에 항목 추가 (필수):
   ```
   ## YYYY-MM-DD — [작업명]
   - 한 것 (1~3줄)
   - 다음 결정 필요한 것 (있으면)
   - 미완료·보강 필요 (있으면)
   ```
2. 새로운 의사결정이 있었으면 DECISIONS.md에 D-XXX 추가
3. git commit (한글 컨벤션 유지)

### 보고 시 (claude.ai로 결과 가져갈 때)
- WORKLOG.md 신규 추가분만 복사해서 첨부 가능하게 정리
- "이번 세션 요약 3줄"을 별도로 제공

### 금지
- WORKLOG 업데이트 누락 (작업했는데 기록 X) = 다음 세션 비용 폭증
- 결정 사항을 코드 주석에만 남기기 (DECISIONS.md에도 반드시)
